# Data Policy

Do not commit raw face datasets, restricted personal data, model weights, or
large generated outputs by default.

Track manifests, hashes, preprocessing versions, access metadata, and license /
privacy constraints required for reproducibility.
